[tool.pytest.ini_options]
# Put the src/ layout package on the import path for tests.
pythonpath = ["src"]
testpaths = ["tests"]

[tool.black]
line-length = 100
target-version = ["py310"]

[tool.isort]
profile = "black"
line_length = 100
